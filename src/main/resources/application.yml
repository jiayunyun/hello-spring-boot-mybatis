spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/wence
      username: root
      password: legalsystem
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.jdbc.Driver

mybatis:
  type-aliases-package: com.jy.hello.spring.boot.mybatis.entity
  mapper-locations: classpath:mapper/*.xml

